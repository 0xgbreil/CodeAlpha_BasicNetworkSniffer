# CodeAlpha_BasicNetworkSniffer
A simple network packet sniffer built with Python and Scapy. It captures network packets and displays useful information such as source/destination IP addresses, MAC addresses, ports, protocol, packet size, and payload.
